# Uncomment this line to define a global platform for your project
platform :ios, '8.0'

use_frameworks!

def shared_pods
end

target 'CirculateSwift' do
shared_pods
pod 'Reveal-SDK', :configurations => ['Debug']
end
